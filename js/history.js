
function showBoard(whiteObj, blackObj, scene, orbitControls){

  // let boardGeo = new THREE.BoxGeometry(20,20,1);
  // let boardMat = new THREE.MeshStandardMaterial({
  //       color: 0xa5a5a5
  //     });
  //
  // let board = new THREE.Mesh(boardGeo, boardMat);
  // board.position.set(orbitControls.object.position.x+20, 1.5, orbitControls.object.position.z+20);
  // scene.add(board);

}
